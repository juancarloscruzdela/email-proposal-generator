{
  "name": "proposal-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained local proposal generator",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
